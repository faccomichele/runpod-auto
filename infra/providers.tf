# The RunPod provider normally reads RUNPOD_API_KEY from the environment.
# If your provider version needs an explicit key, uncomment the block below
# and pass -var="runpod_api_key=..." (or set TF_VAR_runpod_api_key).
#
# provider "runpod" {
#   api_key = var.runpod_api_key
# }
